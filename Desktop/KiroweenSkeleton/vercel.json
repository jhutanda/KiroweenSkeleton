{
  "buildCommand": "npm install --workspaces && cd apps/notes-app && npm run build",
  "outputDirectory": "apps/notes-app/dist",
  "installCommand": "npm install --workspaces --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
